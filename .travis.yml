language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulp' ]; then cd .. && eval "mv $currentfolder generator-gulp" && cd generator-gulp; fi
